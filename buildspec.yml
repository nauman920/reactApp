version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo Installing dependencies...
      - npm ci --legacy-peer-deps

  build:
    commands:
      - npm run build

artifacts:
  files:
    - '**/*'
  base-directory: build
  discard-paths: no